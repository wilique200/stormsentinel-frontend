# Copy to .env.local and fill in. Never commit .env.local itself.

# Your deployed Render backend URL (no trailing slash). Use
# http://localhost:8000 for local development against a local backend.
NEXT_PUBLIC_API_URL=http://localhost:8000
